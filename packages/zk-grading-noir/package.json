{
  "name": "@proofmark/zk-grading-noir",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json && mkdir -p dist/artifacts && cp src/artifacts/*.json dist/artifacts/",
    "lint": "eslint src --ext .ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@noir-lang/acvm_js": "1.0.0-beta.20",
    "@noir-lang/noir_js": "1.0.0-beta.20",
    "@noir-lang/noirc_abi": "1.0.0-beta.20",
    "@proofmark/crypto": "workspace:*",
    "@proofmark/shared": "workspace:*"
  }
}
